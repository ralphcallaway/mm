machine:
  environment:
      CI: true
dependencies:
  pre:
    - sudo apt-get install python-keyring
    - sudo apt-get install python-gnomekeyring
    - sudo pip install jinja2 suds keyring markupsafe pyyaml requests
test:
  override:
    - cd test
    - python suite.py:
        timeout: 1200